以下是从 PDF **目录相关页**抽取的原文（可能很长、含双栏与噪声）。请**只**按 system 要求输出**一段** JSON，不要其它说明。`full_toc` 使用树形 `nodes`；`sections` 为章级粗块 + `suggested_chunk_type`；子级只在 `full_toc` 的 `children` 中展开，**不要**在 `sections` 里为每条子行重复一条。

{toc_text}
